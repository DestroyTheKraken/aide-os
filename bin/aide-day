#!/usr/bin/env bash
# Into the Sequence — open calm LFCS Day Start GUI
set -euo pipefail
AIDE_ROOT="${AIDE_ROOT:-$HOME/AIDE_OS}"
export AIDE_ROOT
exec python3 "$AIDE_ROOT/scripts/learning/day-start-app.py" "$@"
