# Console (leftover)

A separate browser dashboard from an earlier experiment. **The Student seat is `aide-day`, not this folder.**

If you still want to look at it:

```bash
cd ~/AIDE_OS/console
python3 -m http.server 8765 --bind 127.0.0.1
```

Then open http://127.0.0.1:8765/. Treat it as leftover until `aide-review` marks it Keep.

