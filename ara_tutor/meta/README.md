# Sync metadata (generated)

| File | Generator | Purpose |
|------|-----------|---------|
| `indexing-manifest.json` | `lfcs-ara-sync.sh` | Content hash for RAG idempotency |
| `last-sync-state.json` | `lfcs-ara-sync.sh` | Day/cycle/hash for Modelfile regen |

Both files are gitignored. Created in PR 7.