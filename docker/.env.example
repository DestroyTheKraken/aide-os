# Copy to docker/.env or ${DEPLOY_DIR}/.env — never commit real secrets.
FORGE_BIND_IP=100.81.13.95
LFCS_ROOT=/home/kraken/Projects/aios-ed
FORGE_PUID=1000
FORGE_PGID=1000
FORGE_WEB_USER=lfcs
FORGE_WEB_PASSWORD=
TZ=America/Chicago

# Open WebUI API (host-side sync only — NOT compose environment)
OPENWEBUI_API_KEY=
OPENWEBUI_SECRET_KEY=lfcs-openwebui-tailnet

# RAG tuning (PR 12)
RAG_TOP_K=5
ENABLE_RAG_WEB_SEARCH=false